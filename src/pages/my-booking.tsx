import { Container } from '@ns-ui/core';

const mybookings = () => {
    return (
        <>
            <Container size="lg">
                <div>mybookings</div>
            </Container>
        </>
    );
};

export default mybookings;
